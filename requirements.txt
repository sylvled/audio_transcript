# Audio Transcript — Dépendances
# Installation automatique via setup.bat / setup.sh
# Installation manuelle (dans .venv activé) :
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt

faster-whisper>=1.0.0
pyannote.audio>=3.1.0
anthropic>=0.40.0
python-dotenv>=1.0.0
librosa>=0.10.0
groq>=0.9.0
